--SETUP--
# docker-compose
In services create one for dbt and postgres, postgres 15 and dbt version 1.8

# docker-profile
create profiles.yml

# start containers
docker compose up -d # d = detached = dont lock terminal to docker outputs

# initalize dbt
docker compose exec dbt bash
dbt init my_project --skip-profile-setup
docker compose exec dbt dbt debug

# verify
docker compose exec dbt dbt debug

# setup environment
can you give me the standard structure for a dbt project9:27 PMClaude responded: For learning purposes you'll mostly work in models/ and seeds/.dbt_project/
├── dbt_project.yml
├── profiles.yml
├── models/
│   ├── staging/       ← raw data cleanup
│   ├── intermediate/  ← joins/logic
│   └── marts/         ← final tables for reporting
├── seeds/             ← CSV files
├── tests/             ← data quality tests
├── macros/            ← reusable SQL functions
└── target/            ← compiled output (auto-generated, don't touch)

--WHY DBT--
# 